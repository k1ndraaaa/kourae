services:
  # postgres:
  #   image: postgres:17
  #   container_name: postgresql
  #   restart: unless-stopped
  #   environment:
  #     POSTGRES_USER: ${PG_USER}
  #     POSTGRES_PASSWORD: ${PG_PASSWORD}
  #     POSTGRES_DB: ${PG_DB}
  #     PGDATA: /var/lib/postgresql/data
  #   ports:
  #     - "5432:5432"
  #   volumes:
  #     - pgdata:/var/lib/postgresql/data
  #   networks:
  #     - vntwrk

  # minio:
  #   image: minio/minio:RELEASE.2023-03-20T20-16-18Z
  #   container_name: minio
  #   ports:
  #     - "9000:9000"
  #     - "9001:9001"
  #   environment:
  #     MINIO_ROOT_USER: ${MO_USER}
  #     MINIO_ROOT_PASSWORD: ${MO_PASSWORD}
  #   command: server /data --console-address ":9001"
  #   volumes:
  #     - ./minio:/data
  #   restart: unless-stopped

  # redis:
  #   image: redis:7
  #   container_name: redis
  #   restart: unless-stopped
  #   ports:
  #     - "6379:6379"
  #   volumes:
  #     - redisdata:/data
  #   networks:
  #     - vntwrk

  libretranslate:
    image: libretranslate/libretranslate
    container_name: libretranslate
    restart: unless-stopped
    ports:
      - "5500:5500"
    environment:
      LT_LOAD_ONLY: en,es,fr,de
    volumes:
      - ltdata:/home/libretranslate/.local
    networks:
      - vntwrk

volumes:
  pgdata:
  redisdata:
  ltdata:

networks:
  vntwrk:
    driver: bridge